# Tratează PDF-urile prin Git LFS (nu e nevoie de 'binary' aici)
*.pdf filter=lfs diff=lfs merge=lfs -text

# Normalizează toate fișierele text la LF
* text=auto eol=lf

# Exclude alte fișiere binare de la conversii
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.exe binary
*.zip binary
*.mp4 binary
*.mov binary
*.docx binary
*.pptx binary
